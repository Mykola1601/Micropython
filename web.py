try:  import usocket as socketexcept:  import socketfrom machine import Pinled = Pin(2, Pin.OUT)from  page import web_pageimport networkimport espesp.osdebug(None)import gcgc.collect()import ressid = 'MicroPython-AP'password = '16011986'ap = network.WLAN(network.AP_IF)ap.active(True)ap.config(essid=ssid, password=password)while ap.active() == False:  print('.')  time.sleep(0.5)print('AP successful')print(ap.ifconfig())def url_parse(url):    l = len(url)    data = bytearray()    i = 0    while i < l:        if url[i] != '%':            d = ord(url[i])            i += 1        else:            d = int(url[i+1:i+3], 16)            i += 3        data.append(d)    return data.decode('utf8')        #web_page():s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)s.bind(('', 80))s.listen(5)while True:        conn, addr = s.accept()  print('Got a connection from %s' % str(addr))  request = conn.recv(1024)  request = str(request)  # print('Content = %s' % request)  led_on = request.find('/?led=on')  led_off = request.find('/?led=off')  if led_on == 6:    print('LED ON')    led.value(1)  if led_off == 6:    print('LED OFF')    led.value(0)        if led.value() == 1:    gpio_state="ON"  else:    gpio_state="OFF"        text = url_parse(request)  #print(text.find("USERNAME"))  print (text[text.find("USERNAME"):])    #text = "USERNAME=TopAutoWiFi&PASSWORD=dt4e4fe&ID=Esp01&ADRESS=NewYork&URL=http%3A%2F%2F1.1601.zzz.com.ua%2F&SUBMIT=ENTER"  '''USERNAME=PahNah&PASSWORD=1234567890&ID=ID_esp&ADRESS=pon&URL=https://usefull.com.ua&SUBMIT=ENTER'''  i = re.search(r"USERNAME=+\w+&PASSWORD=+\w+&ID=+\w+&ADRESS=+\w+&URL=+\w+[\w://.]+", text[text.find("USERNAME"):])  i = re.search(r"USERNAME=+\w")  if i:      print(i.group(0))  #for match in iterator:  #print(i.group())         response = web_page(gpio_state)  conn.send('HTTP/1.1 200 OK\n')  conn.send('Content-Type: text/html\n')  conn.send('Connection: close\n\n')  conn.sendall(response)  conn.close()